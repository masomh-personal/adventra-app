# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api

# Public Supabase URL (required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Public Supabase Anonymous Key (required for client-side operations)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Storage Bucket Name (required for image uploads)
NEXT_PUBLIC_SUPABASE_BUCKET=member_pictures

# Supabase Service Role Key (required for server-side operations)
# ⚠️ WARNING: This is a private key. Never commit this to version control!
# Only use this in server-side code or build-time environment variables.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Git Information (optional - for displaying build info in footer)
# These are typically set automatically by CI/CD or deployment platforms
NEXT_PUBLIC_GIT_SHA=dev-sha
NEXT_PUBLIC_GIT_BRANCH=dev-branch
